/**
 * Chat runtime config.
 * Set CHAT_WS_ENDPOINT to your deployed Cloudflare Worker endpoint.
 * Example: "wss://tesla-lock-chat.<subdomain>.workers.dev/chat/global"
 */
window.CHAT_WS_ENDPOINT = window.CHAT_WS_ENDPOINT || "";
